name: jobs_done10

dependencies:
  - click
  - jenkins-webapi
  - mock
  - pytest
  - pyyaml
  {% if os.sys.platform.startswith('linux') %}
  - git
  {% endif %}

environment:
  PYTHONPATH:
    - {{ os.path.join(root, 'source', 'python') }}
  PATH:
    - {{ os.path.join(root, 'bin') }}
